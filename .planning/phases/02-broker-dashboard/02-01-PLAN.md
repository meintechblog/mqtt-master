---
phase: 02-broker-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/services/sys-broker-service.js
  - server/routes/ws-dashboard.js
  - server/index.js
  - tests/sys-broker-service.test.js
  - tests/ws-dashboard.test.js
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, DASH-05, DASH-06, DASH-07]

must_haves:
  truths:
    - "SysBrokerService subscribes to $SYS/# and aggregates metrics into a state object"
    - "WebSocket endpoint at /ws/dashboard pushes sys_state and connection_status messages"
    - "New WebSocket clients receive full current state on connect"
    - "Backend batches $SYS updates with 500ms debounce before pushing to clients"
    - "Connection status changes are pushed to WebSocket clients"
  artifacts:
    - path: "server/services/sys-broker-service.js"
      provides: "SysBrokerService that aggregates $SYS topic data"
      exports: ["SysBrokerService"]
    - path: "server/routes/ws-dashboard.js"
      provides: "WebSocket route handler for /ws/dashboard"
      exports: ["default (Fastify plugin)"]
    - path: "tests/sys-broker-service.test.js"
      provides: "Unit tests for SysBrokerService"
      contains: "describe.*SysBrokerService"
    - path: "tests/ws-dashboard.test.js"
      provides: "Integration tests for WebSocket dashboard endpoint"
      contains: "describe.*ws-dashboard"
  key_links:
    - from: "server/services/sys-broker-service.js"
      to: "server/services/mqtt-service.js"
      via: "listens to MqttService 'message' event, filters $SYS topics"
      pattern: "mqttService\\.on.*message"
    - from: "server/routes/ws-dashboard.js"
      to: "server/services/sys-broker-service.js"
      via: "reads sysBrokerService.getState() and listens to 'update' events"
      pattern: "sysBrokerService\\.(getState|on)"
    - from: "server/index.js"
      to: "server/services/sys-broker-service.js"
      via: "creates SysBrokerService, decorates on app"
      pattern: "app\\.decorate.*sysBrokerService"
---

<objective>
Create the backend data pipeline for the broker dashboard: a SysBrokerService that subscribes to Mosquitto $SYS/# topics via MqttService, aggregates metrics into a structured state object, and a WebSocket route at /ws/dashboard that pushes real-time updates to connected frontend clients.

Purpose: Provides the data layer that the frontend dashboard (Plan 02-02) consumes. Without this, the dashboard has no live data.
Output: SysBrokerService, WebSocket route, tests for both.
</objective>

<execution_context>
@/Users/hulki/.claude/get-shit-done/workflows/execute-plan.md
@/Users/hulki/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-broker-dashboard/2-CONTEXT.md
@.planning/phases/01-webapp-shell-and-core-services/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs from Phase 1. -->

From server/services/mqtt-service.js:
```javascript
export class MqttService extends EventEmitter {
  async connect()                          // Connects to broker, resolves after connect or 5s timeout
  subscribe(topic)                         // Subscribe to MQTT topic
  publish(topic, payload, opts)            // Publish to MQTT topic
  isConnected()                            // Returns boolean
  // Events: 'connected', 'disconnected', 'message' ({topic, payload, timestamp})
}
```

From server/index.js:
```javascript
// Services decorated on Fastify app:
app.mqttService    // MqttService instance
app.configService  // ConfigService instance
// @fastify/websocket already registered (line 29)
```

WebSocket message format (from CONTEXT.md):
```json
{ "type": "sys_state", "data": { ...flatMetrics }, "topics": { ...hierarchy } }
{ "type": "connection_status", "connected": true|false }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SysBrokerService with $SYS aggregation and debounced updates</name>
  <files>server/services/sys-broker-service.js, tests/sys-broker-service.test.js</files>
  <behavior>
    - Constructor accepts mqttService, calls mqttService.subscribe('$SYS/#')
    - Filters incoming messages to only process topics starting with '$SYS/'
    - Parses numeric $SYS values (e.g., "3" -> 3, "2.4" -> 2.4), keeps strings as-is (e.g., version)
    - getState() returns { data: {flatMetrics}, topics: {hierarchy} } matching CONTEXT.md format
    - data object maps: $SYS/broker/clients/connected -> clients_connected, $SYS/broker/messages/received -> messages_received, $SYS/broker/messages/sent -> messages_sent, $SYS/broker/load/publish/received/1min -> publish_received_1min, $SYS/broker/load/publish/sent/1min -> publish_sent_1min, $SYS/broker/subscriptions/count -> subscriptions_count, $SYS/broker/heap/current -> heap_current, $SYS/broker/heap/maximum -> heap_maximum, $SYS/broker/load/messages/received/1min -> load_received_1min, $SYS/broker/load/messages/received/5min -> load_received_5min, $SYS/broker/load/messages/received/15min -> load_received_15min, $SYS/broker/version -> version, $SYS/broker/uptime -> uptime (in seconds as number)
    - topics object builds hierarchical tree: "$SYS/broker/clients/connected" with value "3" -> { "$SYS": { "broker": { "clients": { "connected": "3" } } } }
    - Emits 'update' event with full state after 500ms debounce (batches rapid $SYS arrivals)
    - Emits 'connection_status' with { connected: boolean } when mqttService emits 'connected' or 'disconnected'
    - isConnected() delegates to mqttService.isConnected()
  </behavior>
  <action>
    Write tests first (RED), then implement SysBrokerService (GREEN).

    SysBrokerService extends EventEmitter. Constructor takes mqttService as dependency.
    - On construction: call mqttService.subscribe('$SYS/#'), attach message listener
    - Internal state: _data (flat metrics object), _topics (hierarchical tree object)
    - TOPIC_MAP constant: maps full $SYS topic paths to flat key names (e.g., '$SYS/broker/clients/connected' -> 'clients_connected')
    - On message: if topic starts with '$SYS/', update _data via TOPIC_MAP lookup (parse as number if possible), update _topics by splitting topic on '/' and building nested object. Set a 500ms debounce timer (clearTimeout/setTimeout) before emitting 'update' with { data: this._data, topics: this._topics }
    - On mqttService 'connected'/'disconnected': emit 'connection_status' with { connected: bool }
    - getState(): returns { data: {...this._data}, topics: {...this._topics} }

    Use a mock MqttService (EventEmitter with subscribe/isConnected stubs) in tests to avoid real MQTT connections.
  </action>
  <verify>
    <automated>npx vitest run tests/sys-broker-service.test.js</automated>
  </verify>
  <done>SysBrokerService aggregates $SYS metrics into flat data + hierarchical topics, debounces updates at 500ms, forwards connection status. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WebSocket /ws/dashboard route and server wiring</name>
  <files>server/routes/ws-dashboard.js, server/index.js, tests/ws-dashboard.test.js</files>
  <behavior>
    - GET /ws/dashboard upgrades to WebSocket
    - On client connect: sends { type: "sys_state", data: {...}, topics: {...} } with current state
    - On client connect: sends { type: "connection_status", connected: bool } with current status
    - On sysBrokerService 'update' event: broadcasts { type: "sys_state", ...state } to all connected clients
    - On sysBrokerService 'connection_status' event: broadcasts { type: "connection_status", ...status } to all connected clients
    - Disconnected clients are removed from broadcast list (no errors on send to closed sockets)
    - server/index.js creates SysBrokerService(mqttService), decorates as app.sysBrokerService, registers ws-dashboard route
  </behavior>
  <action>
    Write tests first (RED), then implement (GREEN).

    **server/routes/ws-dashboard.js:** Export as a Fastify plugin (async function with app, opts).
    - Register a GET route at '/ws/dashboard' with { websocket: true }
    - Track connected clients in a Set
    - On connection: add socket to Set, send full state from app.sysBrokerService.getState() as { type: "sys_state", ...state }, send { type: "connection_status", connected: app.sysBrokerService.isConnected() }
    - On socket close: remove from Set
    - Listen to app.sysBrokerService 'update' event: JSON.stringify and send to all clients in Set (wrap in try/catch, remove dead sockets)
    - Listen to app.sysBrokerService 'connection_status' event: same broadcast pattern
    - Use safeSend helper: try socket.send(msg), catch -> remove from Set

    **server/index.js modifications:**
    - Import SysBrokerService from './services/sys-broker-service.js'
    - Import wsDashboard from './routes/ws-dashboard.js'
    - After mqttService.connect(): create sysBrokerService = new SysBrokerService(mqttService)
    - app.decorate('sysBrokerService', sysBrokerService)
    - app.register(wsDashboard)

    **Tests:** Use Fastify inject or actual WebSocket client (ws package already available as transitive dep of @fastify/websocket). Create a minimal Fastify app with mocked sysBrokerService to test:
    - Client receives initial state on connect
    - Client receives broadcast on sysBrokerService 'update' event
    - Client receives connection_status broadcast
  </action>
  <verify>
    <automated>npx vitest run tests/ws-dashboard.test.js</automated>
  </verify>
  <done>WebSocket route at /ws/dashboard sends full state on connect, broadcasts sys_state and connection_status updates. server/index.js wires SysBrokerService and registers route. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (existing + new)
- Start server with `node server/index.js`, connect via wscat or browser DevTools: `new WebSocket('ws://localhost:3000/ws/dashboard')` -- receives JSON messages with type "sys_state"
</verification>

<success_criteria>
- SysBrokerService subscribes to $SYS/#, aggregates into flat data + hierarchical topics
- 500ms debounce batches rapid $SYS updates
- WebSocket at /ws/dashboard pushes full state on connect and broadcasts changes
- Connection status changes propagated to WebSocket clients
- All new tests pass alongside existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-broker-dashboard/02-01-SUMMARY.md`
</output>
